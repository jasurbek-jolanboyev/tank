# Dataset contract

Use YOLO normalized labels (`class x_center y_center width height`) beside images
or in parallel `images/` and `labels/` directories. Keep source/license records
for every imported dataset. Never mix frames from one video across train and test.

