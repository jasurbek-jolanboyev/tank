# Testing

- Portable C++: CMake + CTest.
- Jetson DEV mode: Python `unittest`, REST process probe and WebSocket probe.
- Flutter: `flutter analyze` and `flutter test`.
- Dataset: `verify_dataset.py` in the training virtual environment.
- Hardware: ESP-IDF build/flash, TF02-Pro UART, GPIO and Jetson/TensorRT remain
  explicitly hardware-gated.
